pipeline {
	agent {
		node {label 'python'}
	}
	environment {
		APP_NAME = 'python-webapp'
		GIT_REPO = ''
		GIT_BRANCH = 'master'
		DEV = 'dev'
		STAGE = 'stage'
		TEMPLATE_NAME = 'python'
		ARTIFACT_FOLDER = "target"
		PORT = 8080
	}
	stages {
		stage('Get Latest Code'){
			git branch:
		}
	}
}

